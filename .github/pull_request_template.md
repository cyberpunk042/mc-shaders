<!--
The house style: a claim carries its evidence. Say what you changed, and say what
would fail if it stopped being true.
-->

## What this changes

## How it was verified

<!--
Paste the output rather than describing it. For an API detail, say where you read the
signature and on which version — this project does not guess at APIs.

If you added a test, say what you saw when you broke the fix on purpose and confirmed
it went red for the right reason. A test that passes both ways reads as coverage and
is worse than none.
-->

## What is still unproven

<!--
Optional but valued. Much of this repository compiles against the real 26.2 API and has
never been observed running; saying which half of that your change is in keeps the
status tables honest.
-->
